function App() {
  return (
    <main className="app-shell">
      <section className="intro-panel" aria-labelledby="app-title">
        <p className="eyebrow">Interactive decimal abacus</p>
        <h1 id="app-title">Abacus workspace</h1>
        <p>
          The React app shell is ready for the abacus model, board, controls,
          and tests planned in the remaining issues.
        </p>
      </section>
    </main>
  );
}

export default App;
